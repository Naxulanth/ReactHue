import styled from 'styled-components';

import EditableLabel from 'react-inline-editing';

const WidgetTitle = styled(EditableLabel)`
font-size: 22px;
height: 30px;
line-height: 30px;
display:inline-block;
`;

export default WidgetTitle;